# CodeM Web App demo

这是从 `codem-web-app` 当前工作区生成的公开可部署静态演示包。

- 页面使用 Mock 数据模式，不依赖内部 API。
- 仓库只保存构建后的页面资源，不包含内部源码、项目文档或 Git 历史。
- `npm run build` 会把 `site/` 复制到 Vercel 使用的 `dist/`。

## 本地预览

```bash
npm run build
npm run preview
```
